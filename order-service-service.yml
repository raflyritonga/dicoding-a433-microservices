# NodePort service to expose order service externally
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: e-commerce
spec:
  type: NodePort
  selector:
    app: order-service
  ports:
    - port: 3000
      targetPort: 3000
      # Custom NodePort for consistent external access
      nodePort: 30000
